apiVersion: v1
kind: Service
metadata:
  name: prometheus-external
  namespace: observability
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30090
    targetPort: 9090
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: prometheus
